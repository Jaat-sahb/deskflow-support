{
  "name": "deskflow-support",
  "version": "1.0.0",
  "description": "Root package for Railway deployment",
  "scripts": {
    "postinstall": "cd backend && npm install",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
